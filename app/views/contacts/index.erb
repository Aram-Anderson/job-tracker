<h1>All Contacts</h1>

<div class="row">
  <% @contacts.each do |contact| %>
  <ul class="col s3">
    <li><%= link_to contact.name, contact_path(contact) %></li>
    <li><%= link_to contact.company.name, company_path(contact.company) %></li>
    <% end %>
  </ul>
</div>
