<h1>All Categories</h1>
<ul>
<% @categories.each do |category| %>
<li class="category_<%= category.id %>"><%= link_to category.name, category_path(category) %><br>
<%= link_to "Delete", category_path(category), method: 'delete' %></li>
<% end %>
</ul>
